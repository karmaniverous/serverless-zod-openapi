pre-commit:
  scripts:
    update-archive:
      runner: bash
prepare-commit-msg:
  scripts:
    add-issue:
      runner: bash
post-commit:
  scripts:
    update-archive:
      env:
        ARCHIVE_NAME: 'archive.tar'
